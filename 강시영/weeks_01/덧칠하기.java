package AlgorithmStudy;

/**
 * @title  : 덧칠하기
 * @author : SI YOUNG
 */

/*
 public int solution(int n, int m, int section[], size_t section_len) {
    int answer = 0;
    for(int i = 0; i < section_len; i++){
        answer++;
        int count = 0;
        for(int j = i + 1; j < section_len; j++){
            if(section[i] + (m - 1) >= section[j])
                {
                    count++;
                }
        }
        i += count;
    }
    return answer;
 }
*/

